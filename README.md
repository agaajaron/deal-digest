# deal-digest

Deal analysis and digest application with ML components.

## Projects



## Project Structure

```
<project>/
├── data/
├── notebooks/
├── src/
├── models/
└── requirements.txt
```

The `src/*.py` files are cell-marker format compatible with VS Code and Jupyter interactive mode.
